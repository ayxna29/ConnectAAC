[build]
  base = "."              # your repo root is the Flutter folder
  publish = "build/web"   # Flutter outputs the web files here
  command = "flutter build web"

[build.environment]
  VITE_API_URL = "https://connectaac.onrender.com"
